# Copy to .env and fill in. All paths are relative to project root (pbm_research_agent).
OPENAI_API_KEY=paste_your_openai_api_key
PORT=8000
LOG_LEVEL=INFO
# DATABASE_URL=   # Leave empty for SQLite at data/chat.db
